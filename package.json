{
  "name": "indiamart-lead-fetcher",
  "version": "1.0.0",
  "description": "Chrome extension for fetching Indiamart leads with secure API integration",
  "scripts": {
    "asbuild:debug": "asc assembly/api.ts --target debug",
    "asbuild:release": "asc assembly/api.ts --target release",
    "asbuild": "npm run asbuild:debug && npm run asbuild:release"
  },
  "dependencies": {
    "assemblyscript": "^0.27.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}